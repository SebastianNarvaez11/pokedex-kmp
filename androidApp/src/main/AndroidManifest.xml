<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Sin esto, en Android toda peticion falla con UnknownHostException y en
         iOS funciona: el fallo solo aparece en una de las dos plataformas. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".PokedexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pokedex">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Pokedex">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                Esquema propio. Funciona siempre, sin dominio ni verificacion,
                pero no es pulsable en la mayoria de apps de mensajeria.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="pokedex" />
            </intent-filter>

            <!--
                App Link. `autoVerify` hace que Android compruebe
                /.well-known/assetlinks.json en el dominio. Si la verificacion
                pasa, el enlace abre la app directamente; si no, Android ensena
                el selector de aplicaciones.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="sebastiannarvaez11.github.io"
                    android:pathPrefix="/pokedex-kmp/pokemon/" />
            </intent-filter>
        </activity>
    </application>

</manifest>
